# Check if running
- name: Check if alertmanager is running
  shell: pgrep -f alertmanager
  register: alert_status
  ignore_errors: yes

# Kill only if running
- name: Kill existing alertmanager if running
  shell: pkill -f alertmanager
  when: alert_status.rc == 0

# Download
- name: Download Alertmanager
  get_url:
    url: https://github.com/prometheus/alertmanager/releases/download/v0.31.1/alertmanager-0.31.1.linux-amd64.tar.gz
    dest: /tmp/alertmanager.tar.gz

# Extract
- name: Extract Alertmanager
  unarchive:
    src: /tmp/alertmanager.tar.gz
    dest: /opt
    remote_src: yes

# Copy binary
- name: Copy Alertmanager binary
  copy:
    src: /opt/alertmanager-0.31.1.linux-amd64/alertmanager
    dest: /usr/local/bin/alertmanager
    mode: '0755'
    remote_src: yes

# Config dir
- name: Create alertmanager config directory
  file:
    path: /etc/alertmanager
    state: directory
    mode: '0755'

# Config file
- name: Create alertmanager config file
  copy:
    dest: /etc/alertmanager/alertmanager.yml
    content: |
      route:
        receiver: "default"
      receivers:
        - name: "default"

# Start (safe)
- name: Start Alertmanager
  shell: |
    nohup /usr/local/bin/alertmanager \
    --config.file=/etc/alertmanager/alertmanager.yml \
    --web.listen-address=":9093" > /tmp/alert.log 2>&1 &
  args:
    executable: /bin/bash